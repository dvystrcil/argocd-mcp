apiVersion: v1
kind: Service
metadata:
  name: argocd-mcp
  namespace: argocd-mcp
  labels:
    app: argocd-mcp
spec:
  selector:
    app: argocd-mcp
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
